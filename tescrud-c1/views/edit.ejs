<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Data Orang</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
<div class="form-box">
  <form action="/edit/<%= index %>" method="POST">
    <label for="name">Nama</label>
    <input type="text" id="name" name="name" value="<%= person.name %>" required>

    <label for="height">Tinggi (cm)</label>
    <input type="text" id="height" name="height" value="<%= person.height %>" required>

    <label for="weight">Berat (kg)</label>
    <input type="text" id="weight" name="weight" value="<%= person.weight %>" required>

    <label for="birthdate">Tanggal Lahir</label>
    <input type="date" id="birthdate" name="birthdate" value="<%= person.birthdate %>" required>

    <label for="married">Status</label>
    <select id="married" name="married">
      <option value="false" <%= person.married ? "" : "selected" %>>Belum Menikah</option>
      <option value="true" <%= person.married ? "selected" : "" %>>Menikah</option>
    </select>

    <input type="submit" value="Update">
  </form>

  <a href="/" class="cancel-btn">Cancel</a>
</div>

</body>
</html>
